# Security Architecture Diagrams

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CLIENT LAYER                              │
│  • Web Browser  • Mobile App  • CLI  • API Client                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ HTTPS (TLS 1.3)
┌─────────────────────────────────────────────────────────────────────┐
│                       CDN / WAF LAYER                               │
│  • Cloudflare  • AWS CloudFront                                     │
│  • DDoS Protection (L3/L4/L7)                                       │
│  • Bot Detection                                                    │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       API GATEWAY                                   │
│  • Load Balancer (ALB/NLB)                                          │
│  • SSL/TLS Termination                                              │
│  • Request Routing                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    SECURITY MIDDLEWARE                              │
├─────────────────────────────────────────────────────────────────────┤
│  1. DDoS Protection                                                 │
│     • IP Blocking                                                   │
│     • Request Pattern Analysis                                      │
│                                                                     │
│  2. Rate Limiting                                                   │
│     • Token Bucket Algorithm                                        │
│     • Per-Tenant Quotas                                             │
│     • Distributed (Redis)                                           │
│                                                                     │
│  3. Authentication                                                  │
│     • JWT Validation                                                │
│     • Session Management                                            │
│     • CSRF Token Check                                              │
│                                                                     │
│  4. Input Validation                                                │
│     • Schema Validation                                             │
│     • Type Checking                                                 │
│     • Length Limits                                                 │
│                                                                     │
│  5. Injection Detection                                             │
│     • Prompt Injection                                              │
│     • SQL Injection                                                 │
│     • XSS Detection                                                 │
│                                                                     │
│  6. Authorization (RBAC)                                            │
│     • Role Check                                                    │
│     • Permission Validation                                         │
│     • Scope Enforcement                                             │
│                                                                     │
│  7. Audit Logging                                                   │
│     • Request Logging                                               │
│     • Security Events                                               │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER                                │
│  • Business Logic                                                   │
│  • Agent Management                                                 │
│  • LLM Integration                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────┬──────────────────┬──────────────────────────────┐
│   DATA LAYER     │  VECTOR LAYER    │    GRAPH LAYER               │
├──────────────────┼──────────────────┼──────────────────────────────┤
│  PostgreSQL      │  QDRANT          │    Neo4j                     │
│  • Row-Level     │  • Collection    │    • Database Isolation      │
│    Security      │    Isolation     │    • Label Filtering         │
│  • Encryption    │  • Tenant Filter │    • Cypher Injection        │
│    at Rest       │                  │      Prevention              │
└──────────────────┴──────────────────┴──────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                     CACHE LAYER                                     │
│  Redis                                                              │
│  • Tenant-Prefixed Keys                                             │
│  • Rate Limit Counters                                              │
│  • Session Storage                                                  │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                   AGENT EXECUTION LAYER                             │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Sandbox 1 (Tenant A)                                         │ │
│  │  • MicroVM (Firecracker)                                      │ │
│  │  • CPU Limit: 2 cores                                         │ │
│  │  • Memory Limit: 2 GB                                         │ │
│  │  • Network: Restricted                                        │ │
│  │  • Timeout: 15 minutes                                        │ │
│  └───────────────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  Sandbox 2 (Tenant B)                                         │ │
│  │  • MicroVM (Firecracker)                                      │ │
│  │  • CPU Limit: 1 core                                          │ │
│  │  • Memory Limit: 512 MB                                       │ │
│  │  • Network: Restricted                                        │ │
│  │  • Timeout: 5 minutes                                         │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      MONITORING LAYER                               │
│  • Prometheus (Metrics)                                             │
│  • Elasticsearch (Logs)                                             │
│  • Grafana (Dashboards)                                             │
│  • PagerDuty (Alerts)                                               │
└─────────────────────────────────────────────────────────────────────┘
```

## Data Flow: Secure Request Processing

```
┌─────────┐
│ Client  │
└────┬────┘
     │ 1. HTTPS Request
     ↓
┌─────────────────┐
│  TLS Handshake  │
│  (TLS 1.3)      │
└────┬────────────┘
     │ 2. Encrypted Request
     ↓
┌──────────────────────┐
│  DDoS Protection     │ ← Check IP blocklist
│  • IP Check          │ ← Rate limit per IP
└────┬─────────────────┘
     │ 3. Allowed
     ↓
┌──────────────────────┐
│  Rate Limiting       │ ← Redis: Check token bucket
│  • Tenant Quota      │ ← Per-minute/hour/day limits
└────┬─────────────────┘
     │ 4. Within Quota
     ↓
┌──────────────────────┐
│  JWT Validation      │ ← Verify signature
│  • Decode Token      │ ← Check expiration
│  • Extract Claims    │ ← Get tenantId, userId, role
└────┬─────────────────┘
     │ 5. Valid Token
     ↓
┌──────────────────────┐
│  CSRF Check          │ ← Validate CSRF token
│  (POST/PUT/DELETE)   │ ← Check session binding
└────┬─────────────────┘
     │ 6. Valid Token
     ↓
┌──────────────────────┐
│  Input Validation    │ ← Schema validation
│  • Type checking     │ ← Length limits
│  • Format validation │ ← Enum validation
└────┬─────────────────┘
     │ 7. Valid Input
     ↓
┌──────────────────────┐
│  Injection Detection │ ← Prompt injection (30+ patterns)
│  • Prompt            │ ← SQL injection
│  • SQL               │ ← XSS
│  • XSS               │
└────┬─────────────────┘
     │ 8. No Threats
     ↓
┌──────────────────────┐
│  RBAC Check          │ ← Check role permissions
│  • Role: DEVELOPER   │ ← Check resource scope
│  • Permission: *     │
└────┬─────────────────┘
     │ 9. Authorized
     ↓
┌──────────────────────┐
│  Set DB Context      │ ← SET app.current_tenant_id
│  • Tenant ID         │ ← Enable RLS filtering
└────┬─────────────────┘
     │ 10. Context Set
     ↓
┌──────────────────────┐
│  Business Logic      │
│  • Process Request   │
│  • Call Database     │ ← RLS auto-filters by tenant
│  • Execute Agent     │ ← In sandbox
└────┬─────────────────┘
     │ 11. Generate Response
     ↓
┌──────────────────────┐
│  XSS Protection      │ ← Sanitize HTML
│  • Encode Output     │ ← Add security headers
└────┬─────────────────┘
     │ 12. Safe Response
     ↓
┌──────────────────────┐
│  Audit Logging       │ ← Log to immutable storage
│  • API_REQUEST       │ ← Timestamp, user, action
└────┬─────────────────┘
     │ 13. Encrypted Response
     ↓
┌─────────┐
│ Client  │
└─────────┘
```

## Multi-Tenant Data Isolation

```
┌───────────────────────────────────────────────────────────────┐
│                    TENANT A (tenant_abc123)                   │
├───────────────────────────────────────────────────────────────┤
│  Database Schema: tenant_abc123                               │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Table: memories                                    │     │
│  │  ┌────────────┬──────────────┬─────────────────┐   │     │
│  │  │ id         │ tenant_id    │ content         │   │     │
│  │  ├────────────┼──────────────┼─────────────────┤   │     │
│  │  │ mem_1      │ abc123       │ Memory 1        │   │     │
│  │  │ mem_2      │ abc123       │ Memory 2        │   │     │
│  │  └────────────┴──────────────┴─────────────────┘   │     │
│  │  RLS Policy: WHERE tenant_id = 'abc123'            │     │
│  └─────────────────────────────────────────────────────┘     │
│                                                               │
│  Vector Store: tenant_abc123_embeddings (QDRANT)              │
│  Cache Keys: tenant:abc123:* (Redis)                          │
│  Graph Database: Tenant_abc123 label (Neo4j)                  │
│                                                               │
│  Sandbox Configuration:                                       │
│  • CPU: 2 cores                                               │
│  • Memory: 2 GB                                               │
│  • Network: Restricted                                        │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                    TENANT B (tenant_xyz789)                   │
├───────────────────────────────────────────────────────────────┤
│  Database Schema: tenant_xyz789                               │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Table: memories                                    │     │
│  │  ┌────────────┬──────────────┬─────────────────┐   │     │
│  │  │ id         │ tenant_id    │ content         │   │     │
│  │  ├────────────┼──────────────┼─────────────────┤   │     │
│  │  │ mem_3      │ xyz789       │ Memory 3        │   │     │
│  │  │ mem_4      │ xyz789       │ Memory 4        │   │     │
│  │  └────────────┴──────────────┴─────────────────┘   │     │
│  │  RLS Policy: WHERE tenant_id = 'xyz789'            │     │
│  └─────────────────────────────────────────────────────┘     │
│                                                               │
│  Vector Store: tenant_xyz789_embeddings (QDRANT)              │
│  Cache Keys: tenant:xyz789:* (Redis)                          │
│  Graph Database: Tenant_xyz789 label (Neo4j)                  │
│                                                               │
│  Sandbox Configuration:                                       │
│  • CPU: 1 core                                                │
│  • Memory: 512 MB                                             │
│  • Network: Restricted                                        │
└───────────────────────────────────────────────────────────────┘

🔒 ISOLATION GUARANTEES:
  • Database queries automatically filtered by tenant_id
  • Vector searches scoped to tenant-specific collections
  • Cache keys prefixed with tenant ID
  • Graph queries include tenant label filter
  • Sandboxes run in separate MicroVMs
```

## Agent Sandbox Architecture

```
┌───────────────────────────────────────────────────────────────┐
│                      HOST SYSTEM                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              Agent Sandbox Manager                     │  │
│  │  • Create/Stop Sandboxes                               │  │
│  │  • Monitor Resource Usage                              │  │
│  │  • Enforce Limits                                      │  │
│  │  • Kill Switch                                         │  │
│  └────────────────────────────────────────────────────────┘  │
│                           ↓                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Firecracker VMM / gVisor                              │  │
│  │  • Lightweight Virtualization                          │  │
│  │  • Fast Boot (< 125ms)                                 │  │
│  │  • Minimal Overhead                                    │  │
│  └────────────────────────────────────────────────────────┘  │
│                           ↓                                   │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │  Sandbox 1  │  Sandbox 2  │  Sandbox 3  │  Sandbox 4  │  │
│  ├─────────────┼─────────────┼─────────────┼─────────────┤  │
│  │ Tenant A    │ Tenant A    │ Tenant B    │ Tenant C    │  │
│  │ Agent 1     │ Agent 2     │ Agent 1     │ Agent 1     │  │
│  │             │             │             │             │  │
│  │ Resources:  │ Resources:  │ Resources:  │ Resources:  │  │
│  │ • CPU: 1    │ • CPU: 2    │ • CPU: 1    │ • CPU: 8    │  │
│  │ • RAM: 512M │ • RAM: 2GB  │ • RAM: 512M │ • RAM: 8GB  │  │
│  │ • Net: 10MB │ • Net: 50MB │ • Net: 10MB │ • Net: 1GB  │  │
│  │ • Time: 5m  │ • Time: 15m │ • Time: 5m  │ • Time: 60m │  │
│  │             │             │             │             │  │
│  │ Network:    │ Network:    │ Network:    │ Network:    │  │
│  │ • Isolated  │ • Isolated  │ • Isolated  │ • Custom    │  │
│  │ • No egress │ • Whitelist │ • No egress │ • Whitelist │  │
│  │             │ allowed     │             │ allowed     │  │
│  │             │             │             │             │  │
│  │ Filesystem: │ Filesystem: │ Filesystem: │ Filesystem: │  │
│  │ • Read-only │ • Read-only │ • Read-only │ • Read-write│  │
│  │ • Temp: 1GB │ • Temp: 5GB │ • Temp: 1GB │ • Temp: 50GB│  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
└───────────────────────────────────────────────────────────────┘

🛡️ ISOLATION MECHANISMS:
  • Separate kernel namespaces (PID, Network, Mount, IPC)
  • cgroups for resource limits (CPU, memory, I/O)
  • seccomp filters for syscall restrictions
  • Separate network namespaces (no cross-tenant traffic)
  • Read-only root filesystem
  • Execution timeouts with kill switch
```
